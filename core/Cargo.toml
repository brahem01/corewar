[package]
name = "core"
version = "0.1.0"
edition = "2024"

[dependencies]
vm = { path = "../vm" }
visualization = { path = "../visualization", optional = true }

[features]
gui = ["visualization/gui"]